version: 1

honeycomb_team: brooke # repo scoped OR could be near the env

deployment_protection_rules:
  staging: # github env name
    queries:
      - honeycomb_environment: stage # honeycomb env to query against
        spec: &queryspec '{
            "time_range": 1800,
            "calculations": [
              {
                  "op": "COUNT"
              }
            ],
            "filters": [
              {
                  "column": "status_code",
                  "op": "=",
                  "value": "500"
              }
            ]
          }'
        threshold:
          operator: '>'
          value: 10
  production:
    queries:
      - honeycomb_environment: prod
        spec: *queryspec
        threshold:
          operator: '>'
          value: 1